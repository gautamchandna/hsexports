/vendor/
.env
.csv
*.csv

# Exported HelpScout data — contains real customer PII, never commit
/conversations/
HELPSCOUT-*/

# Windows/WSL alternate-data-stream metadata — never wanted in git.
*:Zone.Identifier
